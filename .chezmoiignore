# Repo documentation & scripts — these live in the repo only,
# they should NOT be copied into the home directory by `chezmoi apply`.
README.md
install.sh
FONTS.md
COLORS.md
CLAUDE.md
LICENSE
colors

# Retired configs kept for reference only — not deployed to the home dir.
archive
