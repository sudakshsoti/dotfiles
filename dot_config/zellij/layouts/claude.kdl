// Claude Code workspace layout — launch with `zellij --layout claude`.
// Wide agent pane (Claude Code) + a narrow shell beside it for git/tests/logs.
// A custom layout must re-declare the tab-bar/status-bar plugins, otherwise the
// builtin chrome won't load — so the full themed bars from config.kdl are kept.
layout {
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="zellij:tab-bar"
        }
        children
        pane size=2 borderless=true {
            plugin location="zellij:status-bar"
        }
    }

    tab name="claude" focus=true {
        pane split_direction="vertical" {
            // Wide pane, focused on open — a plain shell. Launch `claude` here
            // yourself when ready (it creates its own worktree).
            pane size="70%" focus=true name="agent"
            // revdiff rail — annotatable branch-vs-main review TUI. One-shot:
            // reload with R to pull in the agent's latest edits, quit with q.
            // Quitting drops into a normal interactive shell rather than a re-run
            // prompt. For a *live* (auto-refreshing) diff to stage/discard/commit,
            // run `lg` (lazygit) from this pane — lazygit refreshes on filesystem
            // changes; revdiff is the deeper, snapshot-at-a-time review surface.
            pane size="30%" name="review" command="zsh" {
                args "-c" "revdiff; exec zsh"
            }
        }
    }
}
